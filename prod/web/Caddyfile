{
    auto_https off
}

:80 {
    encode gzip

    # Proxy API calls to FastAPI (strip /gws/api prefix)
    handle_path /gws/api/* {
        reverse_proxy backend:8000
    }

    # Everything else goes to the frontend
    handle {
        reverse_proxy frontend:5173
    }
}
