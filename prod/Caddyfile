{
    auto_https off
}

:80 {
    encode gzip

    @backend {
        path /auth* /users* /docs* /redoc* /openapi.json /health*
    }
    reverse_proxy @backend backend:8000

    handle {
        reverse_proxy frontend:5173
    }
}
