import {Page} from '../ui/page';

const PickerWorker = function () {
    return (
        <Page>
            <div className="pb-5 mb-5">
                <h1 className="h2 mb-1 text-dark">Picker Workbench</h1>
                <p className="text-muted mb-0">This screen will guide operators through picking tasks.</p>
            </div>
        </Page>
    );
};

export default PickerWorker;
